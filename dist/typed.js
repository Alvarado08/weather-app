const titleEl = document.getElementById('title');
let typed = new Typed('#title', {
    stringsElement: '#typed-strings',
    smartBackspace: true,
    typeSpeed: 70,
    backDelay: 700,
    backSpeed: 100,
    loop: true,
    loopCount: 2,
});